# OrderLink Frontend - نظام إدارة طلبيات المستودع

تطبيق الويب الأمامي لنظام OrderLink - واجهة مستخدم حديثة وسريعة لإدارة الطلبيات بين الأقسام والمستودع.

## 🚀 المميزات

- ✅ **واجهة مستخدم حديثة** مبنية على Next.js 14
- ✅ **دعم كامل للعربية** مع RTL
- ✅ **تصميم متجاوب** يعمل على جميع الأجهزة
- ✅ **ثلاث واجهات مخصصة**: للأقسام، المستودع، والسائقين
- ✅ **إدارة الطلبيات** مع تتبع الحالة في الوقت الفعلي
- ✅ **تحميل PDF** للطلبيات
- ✅ **سجل كامل** لجميع التغييرات
- ✅ **نظام مصادقة آمن** مع JWT

## 🛠️ التقنيات المستخدمة

- **Next.js 14** (App Router)
- **TypeScript**
- **Tailwind CSS**
- **shadcn/ui** - مكونات UI
- **React Hook Form** - إدارة النماذج
- **Zod** - التحقق من البيانات
- **Axios** - HTTP Client
- **Lucide Icons** - الأيقونات
- **Framer Motion** - الحركات والانتقالات

## 📋 المتطلبات

- Node.js >= 18
- npm أو yarn
- Backend API يعمل (راجع [OrderLink_HCO_Backend](https://github.com/YOUR_USERNAME/OrderLink_HCO_Backend))

## 🔧 التثبيت والإعداد

### 1. استنساخ المشروع

```bash
git clone https://github.com/YOUR_USERNAME/OrderLink_HCO_Frontend.git
cd OrderLink_HCO_Frontend
```

### 2. تثبيت التبعيات

```bash
npm install
```

### 3. إعداد متغيرات البيئة

```bash
cp .env.example .env.local
```

ثم قم بتحديث الملف `.env.local`:

```env
NEXT_PUBLIC_API_URL="http://localhost:5000/api"
```

### 4. تشغيل التطبيق

```bash
# Development mode
npm run dev

# Build for production
npm run build

# Start production server
npm start
```

التطبيق سيعمل على `http://localhost:3000`

## 🗂️ بنية المشروع

```
OrderLink_HCO_Frontend/
├── app/
│   ├── (auth)/
│   │   └── login/              # صفحة تسجيل الدخول
│   ├── (dashboard-layout)/
│   │   ├── orders/
│   │   │   ├── new/           # إنشاء طلب جديد
│   │   │   ├── manage/        # إدارة الطلبات
│   │   │   ├── history/       # سجل الطلبات
│   │   │   └── delivery/      # طلبات التوصيل
│   │   ├── departments/       # إدارة الأقسام
│   │   ├── warehouses/        # إدارة المستودعات
│   │   ├── items/             # إدارة المواد
│   │   ├── users/             # إدارة المستخدمين
│   │   └── profile/           # الملف الشخصي
│   ├── layout.tsx
│   └── page.tsx
├── components/
│   ├── ui/                    # مكونات shadcn/ui
│   ├── forms/                 # نماذج مخصصة
│   ├── layout/                # مكونات التخطيط
│   └── common/                # مكونات مشتركة
├── lib/
│   ├── api/                   # API clients
│   ├── constants/             # الثوابت
│   ├── utils/                 # دوال مساعدة
│   └── validations/           # Zod schemas
├── types/
│   └── index.ts               # TypeScript types
├── public/
│   ├── images/
│   └── icons/
├── .env.example
├── .gitignore
├── next.config.js
├── tailwind.config.ts
├── tsconfig.json
└── README.md
```

## 📱 الواجهات المتاحة

### واجهة الأقسام
- تسجيل الدخول برقم الهاتف
- إنشاء طلب جديد
- عرض طلبات القسم
- متابعة حالة الطلبات
- عرض السجل التاريخي

### واجهة المستودع
- عرض جميع الطلبات الواردة
- تحديث حالة الطلبات:
  - قيد المراجعة
  - قيد التجهيز
  - جاهز للتسليم
- تحميل PDF للطلبات
- عرض سجل التغييرات الكامل
- إدارة الأقسام والمستودعات
- إدارة المواد

### واجهة السائقين
- عرض الطلبات الجاهزة للتوصيل
- تحديث الحالة إلى "تم التسليم"
- عرض تفاصيل الطلب

## 🎨 التصميم

التطبيق يستخدم نظام الألوان التالي (حسب قواعد المشروع):

| الغرض | الاسم | HEX |
|------|------|-----|
| Primary | Supply Blue | #1E88E5 |
| Primary Dark | Warehouse Navy | #0D47A1 |
| Secondary | Clean Gray | #ECEFF1 |
| Accent | Action Green | #43A047 |
| Warning | Alert Amber | #FFB300 |
| Danger | Status Red | #E53935 |
| Background | Soft White | #FAFAFA |

## 🔐 المصادقة

التطبيق يستخدم JWT للمصادقة. الـ token يتم تخزينه في localStorage ويُرسل تلقائياً مع كل طلب API.

### تدفق المصادقة

1. المستخدم يدخل رقم الهاتف وكلمة المرور
2. Backend يرجع JWT token
3. Token يُخزن في localStorage
4. Token يُرسل في header مع كل طلب:
   ```
   Authorization: Bearer <token>
   ```

## 🌐 النشر على Vercel

### الطريقة السريعة

1. رفع الكود إلى GitHub
2. اذهب إلى [Vercel](https://vercel.com)
3. استورد المشروع من GitHub
4. أضف متغير البيئة:
   ```
   NEXT_PUBLIC_API_URL=https://your-backend-api.com/api
   ```
5. انشر!

### عبر CLI

```bash
# تثبيت Vercel CLI
npm i -g vercel

# تسجيل الدخول
vercel login

# النشر
vercel --prod
```

## 🔗 الربط مع Backend

تأكد من:

1. Backend يعمل ويمكن الوصول إليه
2. `NEXT_PUBLIC_API_URL` في `.env.local` يشير إلى Backend الصحيح
3. Backend يسمح بـ CORS من نطاق Frontend

مثال للتطوير المحلي:
```env
NEXT_PUBLIC_API_URL="http://localhost:5000/api"
```

مثال للإنتاج:
```env
NEXT_PUBLIC_API_URL="https://api.yourdomain.com/api"
```

## 🧪 الاختبار

```bash
# Run tests
npm test

# Run tests with coverage
npm run test:coverage

# Run E2E tests
npm run test:e2e
```

## 🔧 التطوير

```bash
# تشغيل في وضع التطوير
npm run dev

# بناء المشروع
npm run build

# تشغيل النسخة المبنية
npm start

# فحص الكود
npm run lint

# تنسيق الكود
npm run format
```

## 📝 ملاحظات

- التطبيق يدعم RTL بشكل كامل للغة العربية
- جميع النماذج تستخدم React Hook Form مع Zod للتحقق
- التصميم متجاوب ويعمل على جميع أحجام الششات
- يدعم الوضع المظلم (يمكن تفعيله في الإعدادات)

## 🚀 ميزات قادمة

- [ ] إشعارات في الوقت الفعلي (Socket.IO)
- [ ] تطبيق PWA
- [ ] وضع Offline
- [ ] تصدير التقارير Excel
- [ ] لوحة تحكم بالإحصائيات

## 📄 الترخيص

MIT License

## 🤝 المساهمة

هذا المشروع تم تطويره خصيصاً لإدارة طلبيات المستودع في المستشفيات.

## 📞 الدعم

للمزيد من المعلومات عن Backend API، راجع [OrderLink_HCO_Backend](https://github.com/YOUR_USERNAME/OrderLink_HCO_Backend)
